### some tips
```
1. 初始化
git init

2. 克隆到本地
git clone <repos>

3. 创建并提交文件
git add <filename>
git commit -a

4. 设置
git config --global user.email "you@example.com"
git config --global user.name "Your Name"

5. 提交本地master和远程master
git push
git push origin master
```
